# Audit senior 2026-07-03 — 53 findings {'critical': 1, 'high': 12, 'medium': 14, 'low': 26}

[CRITICAL] (embauche_candidat) Table dimona_declarations definie DEUX fois avec des schemas incompatibles -> la moitie du flux Dimona plante en prod
  FICHIER: supabase/migrations/20260620000150_contracts.sql:74 vs supabase/migrations/20260620000790_dimona_declarations.sql:3
  POURQUOI: Deux `CREATE TABLE IF NOT EXISTS dimona_declarations` divergents coexistent dans le repo. Schema A (000150, gagne car migration anterieure + IF NOT EXISTS) a les colonnes `declaration_kind` ('IN'/'OUT'), `start_date` (NOT NULL), `notes`, `reference_number`, status 'declared_onss'/'confirmed'. Schema B (000790) a `kind` ('in'/'out' CHECK), `employer_org_key` (NOT NULL), `declared_start_date`, `note`, `dimona_period_id`, UNIQUE(employee_id,kind), status 'declared'/'cancelled'. Le code applicatif est coupe en deux camps qui ecrivent/lisent des colonnes mutuellement exclusives sur la MEME table. Sous Schema A (le plus probable) : le tableau de bord RH `src/app/rh/dimona/page.tsx:31` (select kind/employer_org_key/declared_start_date/dimona_period_id/note) -> 500 ; `src/app/rh/dimona/actions.ts:55,59,62,70` (kind/declared_start_date/employer_org_key) -> KO ; `src/app/planning/employees/[id]/dimona-actions.ts:30,102` (kind, sans declaration_kind/start_date NOT NULL) -> insert refuse => bouton 'Dimona declaree manuellement' cassé ; `src/app/api/docuseal/webhook/route.ts:304-311,376-384` (upsert onConflict 'employee_id,kind') -> Dimona IN/OUT auto a la signature cassée ; `src/lib/employment-lifecycle.ts:97` (kind) -> Dimona OUT auto a la sortie cassée. La Dimona est une obligation legale belge (declaration ONSS) et une decision humaine obligatoire : ce split-brain fausse/bloque le suivi Dimona de chaque embauche et sortie.
  FIX: Choisir UN seul schema canonique pour dimona_declarations, ecrire une migration ALTER de convergence (renommer declaration_kind<->kind, start_date<->declared_start_date, unifier les valeurs de status, ajouter les colonnes manquantes), puis aligner TOUS les writers/readers (hire-actions, contract-actions, dimona-actions, rh/dimona, docuseal/webhook, employment-lifecycle, crons) sur ces memes noms de colonnes et memes valeurs de status. Supprimer la definition en double.

[HIGH] (auth_i18n_session) IDOR: un candidat peut lire/écrire/finaliser le screening d'un AUTRE candidat (fausse la décision RH)
  FICHIER: src/app/me/screening/actions.ts:55
  POURQUOI: answerScreeningQuestionAction(responseId,...) et submitScreeningResponseAction(responseId) (lignes 55 et 100) utilisent createAdminClient() (service-role, RLS contournée) et ne vérifient QUE if(!user). Le responseId vient du client (screening-form.tsx:84/96, variable `rid`/`responseId` côté navigateur) et n'est jamais recoupé avec le candidate_id du user connecté. N'importe quel utilisateur authentifié (y compris un candidat lambda) peut donc injecter des réponses dans le screening d'un autre candidat (delete+insert sur screening_answers, lignes 80-95) puis appeler submit pour recalculer et ÉCRASER total_score / recommendation / has_red_flag (lignes 142-151) de la victime. Le champ recommendation alimente directement la décision d'embauche RH : un attaquant peut faire recaler un concurrent ou se surnoter. C'est une corruption de données qui fausse une décision RH.
  FIX: Dans les deux actions, après avoir résolu le candidate_id du user (comme getOrCreateMyScreeningResponseAction le fait via admin.from('candidates').eq('profile_id',user.id)), charger la screening_response par id ET vérifier response.candidate_id === candidateDuUser avant tout write ; sinon retourner {ok:false,error:'unauthorized'}. Idem pour answerScreeningQuestionAction (vérifier que le response_id appartient au candidat).

[HIGH] (auth_i18n_session) IDOR: fuite de justificatifs de notes de frais entre employés via getReceiptUrlAction
  FICHIER: src/app/me/expenses/actions.ts:136
  POURQUOI: getReceiptUrlAction(expenseId) n'exige que requireUser() (tout compte authentifié, candidat compris), puis lit expense_reports par id avec createAdminClient() (RLS contournée) et renvoie une signed URL valable 1h vers le justificatif. Aucun contrôle que la note de frais appartient à l'employé appelant. En énumérant/ devinant des expenseId, un utilisateur peut télécharger les reçus (documents financiers/personnels) de n'importe quel collègue — violation RGPD et fuite inter-rôles.
  FIX: Résoudre l'employee.id du user connecté (employees.profile_id = user.id) et n'autoriser le téléchargement que si expense.employee_id === monEmployeeId ; élargir aux rôles RH/manager via requireRole si l'accès back-office est voulu. Refuser sinon.

[HIGH] (conges_absences_swaps) Le solde de congés est toujours faux : le calcul ne compte aucune demande réelle (mismatch de `kind`)
  FICHIER: src/lib/leave-balance.ts:91
  POURQUOI: Le formulaire et l'action stockent `kind` = 'vacation' | 'sick' | 'personal' | 'unpaid' | 'other' (src/app/planning/actions.ts:882 ; libellés src/app/me/time-off/time-off-my-panel.tsx:19). Mais computeBalanceForEmployee filtre `.in('kind', ['paid_leave','conge_paye','annual_leave','leave'])` — AUCUNE de ces valeurs n'est jamais utilisée par l'app. Conséquence : `used_days` et `pending_days` valent TOUJOURS 0, donc `remaining_days` = allocation totale en permanence, quel que soit le nombre de jours réellement pris. La carte solde (leave-balance-card.tsx) affiche donc un solde faux à chaque travailleur, et le cron nouvel-an (leave-balance-new-year/route.ts:37-54) notifie systématiquement 'X j non pris/perdus' avec le total complet. Le pilotage RH des congés payés est entièrement corrompu.
  FIX: Aligner le filtre sur les kinds réellement stockés : compter les jours payés = kind 'vacation' (et exclure 'unpaid'/'sick' selon la politique). Idéalement, normaliser une seule liste de constantes partagée entre le formulaire, l'action d'insertion et leave-balance.ts pour éviter toute divergence future.

[HIGH] (conges_absences_swaps) L'auto-validation des congés ne vérifie jamais le solde disponible (quota non appliqué)
  FICHIER: src/lib/leave-auto-validation.ts:354
  POURQUOI: evaluateLeaveRequestWithParams ne contrôle que préavis, durée max, périodes bloquées et % d'absents. Il ne consulte jamais leave_balances/remaining_days. requestTimeOffAction (src/app/planning/actions.ts:903-922) approuve donc automatiquement une demande même si l'employé n'a plus de jours de congé. Un employé peut auto-valider bien plus que son quota légal belge (ex. plusieurs demandes ≤ maxConsecutiveDays espacées), sans qu'aucun humain ne soit sollicité. Combiné au bug de solde ci-dessus, il n'existe aucun garde-fou sur la consommation des congés.
  FIX: Ajouter une règle 'quota_exceeded' dans le moteur : charger le solde restant (une fois le calcul du solde corrigé) et escalader au manager si (jours demandés cumulés + déjà pris/pending) > total alloué. Ne jamais auto-approuver au-delà du solde.

[HIGH] (conges_absences_swaps) acceptSwap/requestSwap ne vérifient pas la propriété du shift cible → vol de shift d'un tiers
  FICHIER: src/app/me/swaps/actions.ts:362
  POURQUOI: acceptSwapAction charge targetShift via swap.target_shift_id puis, en cas d'auto-validation, transfère ce shift au requester (ligne 387-392) sans jamais vérifier que targetShift.employee_id === me.id (le destinataire qui accepte). requestSwapAction (262-268) n'impose pas non plus que target_shift_id appartienne bien à target_employee_id. Une demande forgée (ou un bug d'UI) avec un target_shift_id appartenant à un TROISIÈME employé fait que, à l'acceptation, le shift de ce tiers est réassigné au requester : le tiers perd son shift, le planning est corrompu, et evaluateSwap a raisonné sur le mauvais propriétaire. Corruption directe du roster.
  FIX: Dans requestSwapAction et acceptSwapAction, vérifier explicitement targetShift.employee_id === swap.target_employee_id (et === me.id à l'acceptation) avant tout transfert ; rejeter sinon.

[HIGH] (embauche_candidat) Le trigger legal ecrase silencieusement les heures/semaine choisies par la RH (seuil 30h vs 38h) -> corruption contrat/paie
  FICHIER: supabase/migrations/20260620000610_legal_rules_engine.sql:58-67
  POURQUOI: normalize_employee_legal derive work_time_kind avec un seuil de 30h (`weekly_hours >= 30 -> 'full'`) puis force `weekly_hours := 38` pour un temps plein, et clampe un temps partiel a [13,30]. Or l'app utilise partout le seuil 38h (`src/app/planning/employees/[id]/contract-terms-actions.ts:64` : `h < 38 ? 'partial' : 'full'`). Consequence : une embauche a 30-37h/sem (hire-actions ne fixe jamais work_time_kind, defaut 38 dans le dialog) -> trigger met 'full' puis reecrit a 38h. Et si la RH corrige a 35h via le popup (work_time_kind='partial'), le trigger clampe a 30h. Toute la bande 30-37h/sem est silencieusement corrompue : les heures reelles du contrat, la paie et le planning ne correspondent plus au choix RH.
  FIX: Aligner le seuil du trigger sur 38h (comme le reste de l'app), ou ne PAS reecrire weekly_hours quand une valeur explicite est fournie (auto-fix uniquement quand null). Harmoniser la derivation full/partial cote DB et cote app sur le meme seuil, et documenter la valeur canonique.

[HIGH] (data_integrity_rls) Fuite IDOR : n'importe quel salarié peut télécharger le justificatif de frais d'un autre (données personnelles/médicales)
  FICHIER: src/app/me/expenses/actions.ts:136-145
  POURQUOI: getReceiptUrlAction n'appelle que requireUser() puis interroge expense_reports via le client ADMIN (createAdminClient, qui bypasse toute RLS) en filtrant uniquement sur l'expenseId passé par le client, SANS vérifier que la note de frais appartient au salarié connecté (aucun .eq('employee_id', monEmployee)). Tout utilisateur authentifié peut donc énumérer des expenseId et obtenir une URL signée vers le reçu d'un collègue. Un justificatif de frais peut contenir des données personnelles sensibles (adresse, achats, reçus médicaux/pharmacie). C'est une violation RGPD directe et une fuite de données entre employés.
  FIX: Après requireUser(), résoudre l'employee du profil connecté (employees.profile_id = user.id) puis charger la note avec .eq('id', expenseId).eq('employee_id', monEmployeeId).maybeSingle(); refuser si absente. Autoriser en plus le contournement uniquement pour requireRole(['admin','rh','manager']). Idem à vérifier sur toute action 'me/*' qui lit via admin client.

[HIGH] (data_integrity_rls) Écrasement stale-form : la sauvegarde admin de la fiche employé efface le BIC et le téléphone saisis par le travailleur
  FICHIER: src/app/planning/employees/actions-admin.ts:80-98
  POURQUOI: La garde anti-écrasement PROTECT couvre iban, address, nrn, etc. mais OMET 'bic' et 'phone', qui sont pourtant des champs éditables par le travailleur en self-service (cf. ALLOWED_FIELDS dans src/app/me/contract-info/actions.ts:11-16 qui contient bic + phone). Scénario réel documenté dans la mémoire projet (stale_form_overwrite) : le travailleur renseigne son BIC via 'complète ton dossier' pendant que le RH a la fiche ouverte (champ BIC vide car chargé avant). Au save admin, STR('') -> null écrase le BIC en base alors que l'IBAN, lui, est préservé. Résultat : donnée bancaire de paie partiellement effacée (BIC utilisé notamment dans le QR EPC de paiement) et téléphone de contact perdu — de façon silencieuse et incohérente.
  FIX: Ajouter 'bic' et 'phone' au tableau PROTECT (et étendre le SELECT de current en conséquence). Idéalement, dériver PROTECT directement de la liste des champs self-service du travailleur pour éviter toute divergence future.

[HIGH] (entites_paie_contrat) UPDATE d'alignement de fiche non vérifié pendant l'envoi du contrat : divergence silencieuse + faux log d'audit
  FICHIER: src/app/planning/employees/[id]/sign-contract-actions.ts:474
  POURQUOI: Dans le bloc 3c (opérateur accepte les discordances), le patch écrit `patch.work_time_kind = effTpl === 'employee_pt' ? 'partial' : 'full'` (ligne 471) — encore 'partial', en conflit avec la contrainte ('full','part') documentée ailleurs. Surtout, l'`await admin.from('employees').update(patch)` (ligne 474) ne teste JAMAIS l'erreur retournée. Si l'UPDATE échoue (violation CHECK sur 'partial', ou autre), la fiche N'EST PAS alignée, mais le code continue, envoie le contrat comme si tout était cohérent, ET insère un activity_log (ligne 476) affirmant « Fiche alignée sur le contrat ». La discordance fiche↔contrat persiste en silence alors que l'audit prétend le contraire — exactement le bug « Sanae » (fiche full vs contrat 24h) que ce module est censé prévenir.
  FIX: Capturer et vérifier l'erreur : `const { error } = await admin.from('employees').update(patch)...; if (error) return { error: error.message };` avant de générer/envoyer le contrat et d'écrire le log. Et écrire la valeur canonique ('part' au lieu de 'partial').

[HIGH] (entites_paie_contrat) Le trigger légal gonfle les heures d'un temps partiel à 38h (corruption de la base contractuelle)
  FICHIER: supabase/migrations/20260620000610_legal_rules_engine.sql:59
  POURQUOI: normalize_employee_legal : quand work_time_kind est NULL et weekly_hours renseigné, il pose `'full'` dès `weekly_hours >= 30` (ligne 59-60), puis la règle weekly_hours_full_38 (activée par défaut : legal_rules.enabled default true, is_rule_enabled renvoie true par défaut) force `new.weekly_hours := 38` (ligne 64). Un temps partiel légitime à 34h dont work_time_kind arrive null (insertion/chemin qui ne pose pas la colonne) est donc silencieusement réécrit en 38h temps plein. Cela corrompt la durée contractuelle, fausse le calcul de rémunération et produit un contrat/Dimona erroné. De plus le trigger écrit 'partial' (ligne 60), en conflit avec la convention 'part' documentée côté app.
  FIX: Aligner le seuil du trigger sur 38h (`>= 38 -> full`), et harmoniser la valeur écrite ('part' vs 'partial') avec la contrainte CHECK réelle de la colonne. Vérifier aussi que forcer 38h sur 'full' n'écrase pas un temps plein contractuel < 38h légitime.

[HIGH] (crons_background) pre-interview-reminder invalide le lien du candidat sans le lui renvoyer et enregistre une fausse relance
  FICHIER: caftan-rh/src/app/api/cron/pre-interview-reminder/route.ts:176-227
  POURQUOI: Le cron regenere le token du pre-entretien (generateToken ligne 176, update ligne 181-189), ce qui INVALIDE immediatement le lien que le candidat possede deja, mais n'envoie AUCUN mail au candidat (aucun appel sendAppMail/sendEmail dans tout le fichier). Il se contente d'inserer une ligne messages direction='outbound' provider='pre_interview_relance' (219-227), de logger une activite 'Relance automatique pre-entretien' (235) et de notifier la RH 'Token renouvele automatiquement' (261-262). Double defaut: (1) le candidat dont le lien etait encore valide (renouvele des J-2) est silencieusement bloque hors du pre-entretien et ne recoit jamais le nouveau lien; (2) le systeme enregistre une trace 'candidat relance/contacte' fausse -> la RH croit le candidat relance alors que rien n'a ete envoye, ce qui fausse la decision de suivi. C'est exactement le bug que interview-reminder a corrige (ligne 99-102) mais ici il persiste.
  FIX: Soit ne PAS roter le token tant qu'aucun envoi n'a lieu (ne prolonger expires_at que si un mail part), soit passer l'envoi par sendAppMail({automated:true}) et ne renouveler le token + inserer la ligne messages QUE si result.ok (sinon distinguer 'blocked'/'failed' comme interview-reminder). La notif RH doit dire 'a renvoyer manuellement' plutot que 'relance automatique'.

[HIGH] (crons_background) rgpd-anonymize : la requete ignore le statut et anonymise tout candidat de plus de 12 mois non lie a un employe (destruction irreversible)
  FICHIER: caftan-rh/src/app/api/cron/rgpd-anonymize/route.ts:41-46
  POURQUOI: Le commentaire d'entete (13-16) definit des criteres precis (status='refused' ET refused_at<12m, OU created_at<12m sans etre employe, OU status NULL <24m). La requete reelle ne filtre QUE sur created_at<12 mois + email pas deja anonymise. Aucun controle de statut: un candidat cree il y a >12 mois mais toujours ACTIF dans le pipeline (relance, re-candidature, offre en cours) voit nom/email/telephone/NRN/IBAN/CV effaces de facon irreversible. Le garde-fou 'lie a un employe' (55-60) utilise maybeSingle() qui renvoie data=null en cas de plusieurs lignes (travailleur reembauche = 2 employees partageant candidate_id) -> le candidat est alors anonymise malgre le lien. La variable twentyFourMonthsAgo (38) est calculee mais jamais utilisee dans une requete. Risque: perte definitive de donnees candidat/employe et non-respect de la politique documentee.
  FIX: Restreindre la requete aux statuts reellement cibles (ex. in('status',['refused','withdrawn']) avec refused_at<12m, plus la branche 24 mois pour status null), et remplacer le maybeSingle() de la verif employe par un select count/limit(1) robuste au multi-lignes. Retirer ou utiliser twentyFourMonthsAgo.

[MEDIUM] (auth_i18n_session) Flux RH cassé en permanence: ajout de formation/certification impossible (stub qui échoue toujours)
  FICHIER: src/app/rh/trainings/actions.ts:25
  POURQUOI: addTrainingAction retourne systématiquement {ok:false,error:'Module formations en reconstruction'} — c'est un stub laissé après perte de l'actions.ts original (pattern de fichiers perdus connu du projet). Conséquence concrète: la RH ne peut enregistrer AUCUNE formation, certification, ni date d'expiration de certificat pour les employés depuis /rh/trainings. Toute la gestion du suivi de formation (et alertes d'expiration légales) est hors service, sans indication que c'est définitif côté fonctionnel.
  FIX: Reconstruire l'insertion réelle (insert dans la table trainings/employee_trainings avec upload du certificat en storage) sous requireRole(['admin','rh']), ou à défaut masquer/désactiver clairement l'entrée de menu pour ne pas laisser croire que la saisie fonctionne.

[MEDIUM] (planning_shifts) La pause prière du vendredi (règle métier dure) n'est PAS appliquée par les 2 générateurs de planning réellement utilisés (solver par site + génération par employé)
  FICHIER: src/app/planning/sites/[code]/actions.ts:774 ; src/app/planning/employees/[id]/generate-actions.ts:491
  POURQUOI: CLAUDE.md impose « Pause vendredi verrouillée dans les plannings (règle métier) ». Or seule la génération legacy /planning/calendar (previewWeekAction -> generateWeekPlan dans src/lib/auto-planning.ts) découpe le shift autour de la pause prière (prayerPauseFor/shiftOverlapsPause). Les deux moteurs actuellement câblés aux boutons de l'Uic ne l'importent NULLE PART : (a) le solver par site previewSitePlanAction / previewOvertimeFillAction / proposeOvertimeCandidatesAction, déclenché par les boutons 'Générer' (generate-button.tsx:72) et 'Boucher les créneaux' (auto-fill-button.tsx:40 -> autoFillSiteContractualAction); (b) la génération par employé generateEmployeeWeekPlanAction (generate-employee-plan-button.tsx:111), qui colle les shifts directement sur les site_needs (ex. vendredi 10:00-20:00) sans jamais réserver la fenêtre de prière. Un grep exhaustif confirme que prayerPause/shiftOverlapsPause n'apparaît que dans auto-planning.ts et generate/actions.ts. Résultat concret : des shifts du vendredi sont générés à cheval sur la pause prière, en violation d'une règle métier explicite. Le break-scheduler.ts pose seulement une pause repas générique centrée au milieu du shift, décalée par site — il ne verrouille pas l'horaire fixe de prière.
  FIX: Extraire l'application de la pause prière (prayerPauseFor + découpe en 2 segments) dans un helper partagé et l'appeler dans les 3 chemins : au moment où previewSitePlanAction/previewOvertimeFillAction pousse un draft du vendredi, et dans generateEmployeeWeekPlanAction lors du placement sur un site_need du vendredi. À défaut, au minimum exclure/scinder tout créneau chevauchant la fenêtre prière avant insert, comme le fait déjà generateWeekPlan.

[MEDIUM] (planning_shifts) Détection d'overlap au commit compare des chaînes 'HH:MM' (draft) et 'HH:MM:SS' (DB) → shift contigu valide silencieusement supprimé
  FICHIER: src/app/planning/sites/[code]/actions.ts:1824
  POURQUOI: Dans commitSitePlanAction l'anti-double-booking teste `d.start_time < e.end && d.end_time > e.start` en comparaison de chaînes. Les drafts portent des heures 'HH:MM' (5 car.) alors que e.end/e.start viennent de la DB en 'HH:MM:SS'. À adjacence exacte (draft qui commence pile quand un shift existant finit : start '10:00' vs end '10:00:00'), la comparaison lexicale '10:00' < '10:00:00' est vraie (préfixe plus court = plus petit), donc un chevauchement est détecté à tort et le draft pourtant légitime (deux shifts consécutifs sur des sites différents le même jour) est compté dans `skipped` et JAMAIS inséré, sans avertissement à l'utilisateur. Le RH croit le créneau couvert alors qu'il ne l'est pas.
  FIX: Normaliser les deux côtés en minutes avant comparaison (réutiliser timeToMin/overlaps déjà présents dans le fichier) plutôt qu'une comparaison de chaînes, ou tronquer systématiquement à slice(0,5) des deux côtés.

[MEDIUM] (pointage_clock) Auto-OUT utilise l'heure de FERMETURE du magasin au lieu de la fin du shift → surcompte massif des heures des temps partiels
  FICHIER: src/app/api/cron/tuya-auto-out/route.ts:163-183
  POURQUOI: Pour un IN resté ouvert, la branche 1 (heures d'ouverture du site via siteHoursFor) est TESTÉE EN PREMIER et pose l'OUT à close_time+30min. La fin de shift (shift.end_time) n'est utilisée qu'en fallback (deadlineMs===null && shift), donc IGNORÉE dès que le site a des site_needs. Un vendeur du shift du matin (fin 14:00) dans un magasin ouvert jusqu'à 18:30 qui oublie de pointer OUT est auto-fermé à 18:30 → +4h30 d'heures fantômes injectées dans clock_entries. Ces heures alimentent directement les prestations/paie et faussent la décision RH (variance, coût, renouvellement).
  FIX: Quand le IN a un shift_id avec end_time connu, prioriser shift.end_time (+ tolérance) sur close_time du site ; ne retomber sur close_time que si aucun shift. Idéalement out = min(shift.end + tolérance, close_time).

[MEDIUM] (pointage_clock) Décalage horaire codé en dur (+02:00 / +2h) dans la clôture et l'inférence → OUT décalés de 1h et reclassements erronés en hiver (CET)
  FICHIER: src/app/api/cron/force-close-orphans/route.ts:117
  POURQUOI: force-close-orphans écrit les timestamps avec `${orphanDay}T${closeTime}:00+02:00` (lignes 117, 122, 131) et tuya-correction-inference.ts fige LOCAL_OFFSET_MS = 2h (ligne 60, utilisé par localHour/localDay lignes 63-68 et 283). L'offset belge est +01:00 en hiver (CET). De fin octobre à fin mars, tous les OUT auto-inférés sont datés 1h trop tard (heures surévaluées) ET localHour renvoie une heure fausse de +1h, ce qui décale les seuils de classification (eventHour>=16, <=12, evening worker) → des pointages corrects sont reclassés IN↔OUT à tort. L'auto-out route, elle, utilise correctement brusselsWallTimeToUtc : incohérence entre deux chemins censés faire la même chose.
  FIX: Remplacer partout l'offset codé en dur par brusselsWallTimeToUtc()/brusselsOffsetMinutes() de src/lib/datetime.ts (calcul DST réel), y compris dans tuya-correction-inference.localHour/localDay.

[MEDIUM] (pointage_clock) L'alternance IN/OUT Tuya ne regarde que source='tuya' → double IN quand le travailleur mélange pointage web et badge
  FICHIER: src/lib/tuya-poll.ts:145-160
  POURQUOI: Le toggle par état ne considère que le dernier tap `source='tuya'` (`.eq('source','tuya')` ligne 148). Or le pointage web (app/me/clock) coexiste et la page /me/clock calcule isClockedIn sur TOUTES sources (page.tsx:60-94). Si un employé clocke IN via l'app puis badge Tuya en partant, lastReal(tuya) est un vieux OUT → alternanceKind='in' → un 2e IN est créé au lieu d'un OUT. Deux sessions ouvertes, heures faussées, alors que Tuya est censé être la source de vérité. Symétriquement le garde-fou ligne 223 force alors 'in', aggravant le cas.
  FIX: Baser le toggle sur le dernier clock_entries de l'employé TOUTES sources confondues (ou au moins tuya+web), pas seulement source='tuya', pour rester cohérent avec le calcul de présence.

[MEDIUM] (conges_absences_swaps) Un congé approuvé ne libère pas / ne réassigne pas les shifts déjà planifiés → shift non couvert
  FICHIER: src/app/planning/actions.ts:903
  POURQUOI: La génération de planning exclut bien les congés approuvés (planning/generate/actions.ts:42-44). Mais quand un congé est auto-validé OU approuvé après coup (requestTimeOffAction / decideTimeOffAction:804), aucun code ne supprime, réassigne ou signale les shifts existants de l'employé tombant pendant le congé (préavis mini 14 j, le planning de la semaine est souvent déjà généré). L'employé reste affiché comme présent sur ses shifts alors qu'il est en congé : le magasin croit le poste couvert, personne n'est alerté, aucune cascade de couverture n'est déclenchée. checkLeaveCoverage est purement informatif et n'agit pas.
  FIX: À l'approbation (auto ou manuelle) d'un congé, détecter les shifts de l'employé sur la période et soit les détacher (employee_id=null + statut 'à couvrir'), soit notifier le manager avec la liste précise des shifts orphelins et un bouton de réaffectation 1-clic.

[MEDIUM] (embauche_candidat) worker_type Dimona (STU/OTH) ignore le statut etudiant auto-declare par le candidat
  FICHIER: src/app/rh/candidates/[id]/hire-actions.ts:297
  POURQUOI: Le worker_type de la Dimona est derive uniquement du type de contrat choisi par la RH dans le dialog (`contractKind === 'Étudiant' ? 'STU' : 'OTH'`), et non du champ `candidates.is_student` que le candidat a explicitement declare dans le formulaire /contract-info. Le defaut du dialog est 'CDD' : si la RH laisse le defaut pour un candidat qui s'est declare etudiant, la Dimona part en 'OTH' (travailleur ordinaire) au lieu de 'STU' -> declaration ONSS erronee, cotisations reduites etudiant perdues, contingent 600h non trace. De plus 'Intérim' et 'Freelance' (proposes dans hire-button.tsx:41) tombent aussi en 'OTH' alors qu'ils exigent un autre worker_type (voire aucune Dimona pour un freelance).
  FIX: Deriver worker_type en croisant `candidate.is_student` ET le type de contrat ; pre-selectionner 'Étudiant' dans le dialog quand is_student=true et alerter en cas de divergence ; mapper Intérim/Freelance vers le worker_type correct (ou bloquer la creation Dimona pour un freelance).

[MEDIUM] (embauche_candidat) Un candidat pre-valide (sans candidature) ne peut jamais etre converti en employe
  FICHIER: src/app/rh/candidates/prevalidated/[id]/page.tsx:1
  POURQUOI: Le flux d'embauche (hireCandidateAction) exige un applicationId et repose sur le trigger promote_application_to_employee declenche par applications.status='hired'. Un candidat pre-valide est cree SANS application (`createPrevalidatedCandidateAction` insere seulement dans candidates). La page de revue pre-valide n'offre aucun bouton 'Embaucher'. Resultat : toutes les infos secretariat social auto-remplies (IBAN, NISS, adresse) et les indisponibilites declarees par ce candidat sont une impasse — rien ne les reprend dans une fiche employee. Le 'rejeu des indispos' de hire-actions.ts:199 lit bien candidate_unavailabilities, mais n'est jamais atteint faute de flux d'embauche pour ce type de candidat.
  FIX: Ajouter sur la page pre-valide un bouton d'embauche qui cree l'employee directement depuis la fiche candidat (sans passer par applications) en reutilisant la logique de copie identite + rejeu indispos + contrat + Dimona, ou generer une application 'shell' pour ce candidat afin de rebrancher le trigger existant.

[MEDIUM] (embauche_candidat) Valeur work_time_kind incoherente : 'partial' ecrit vs 'part' attendu par la contrainte CHECK
  FICHIER: src/app/planning/employees/[id]/contract-terms-actions.ts:48-49
  POURQUOI: contract-terms-actions valide et ecrit `work_time_kind` = 'partial' (ligne 49 rejette tout ce qui n'est pas 'full'/'partial', ligne 64 derive 'partial'), tout comme sign-contract-actions.ts:471 et les triggers DB (000600/000610). Mais src/lib/contract-render-inputs.ts:130-133 documente explicitement que la valeur CANONIQUE est 'part'/'full' (= convention DB + contrainte CHECK) et que 'partial' 'violerait le CHECK'. Si la contrainte CHECK 'part'/'full' existe reellement sur employees.work_time_kind, toute ecriture 'partial' (popup de correction des termes, normalisation legale d'un temps partiel) leve une check_violation et bloque la sauvegarde de l'employe. A minima le code se contredit sur la valeur canonique, ce qui fausse la detection de discordance fiche/contrat.
  FIX: Fixer une seule valeur canonique (aligner sur la contrainte CHECK reelle : soit 'part', soit 'partial') et corriger tous les writers (contract-terms-actions, sign-contract-actions, triggers normalize_employee_legal) pour l'utiliser ; ajouter un test qui insere un temps partiel pour valider contre le CHECK.

[MEDIUM] (notifs_mails_killswitch) Les e-mails candidats (confirmation entretien, accusé de candidature, convocation/rappel/refus/offre) partent via emails.ts = Resend UNIQUEMENT, non configuré → jamais délivrés
  FICHIER: C:/Users/KElba/Documents/GitHub/Formulaire_candidats/caftan-rh/src/lib/emails.ts:6-72
  POURQUOI: sendEmail() n'utilise QUE Resend (getClient() renvoie null si RESEND_API_KEY absente, ligne 48-64 -> retour {skipped:true}). Or le code de l'app affirme lui-même que Resend est désactivé dans cet env (src/app/api/cron/sequences-tick/route.ts:208 « Resend is disabled (no API key) in this env ») et le pipeline principal a migré vers Gmail (sendAppMail). Résultat : sendInterviewConfirmation et sendApplicationAcknowledgement — qui sont des accusés/reçus AUTORISÉS par le kill-switch — ne partent jamais alors que le reste de l'app envoie bien via Gmail. Le candidat ne reçoit ni accusé de candidature ni confirmation d'entretien : flux candidat cassé en silence.
  FIX: Router emails.ts à travers le même pipeline que sendAppMail/sendMailWithAttachments (Gmail SMTP primaire -> Resend -> EmailJS) au lieu de Resend seul, ou remplacer les appels sendInterviewConfirmation/sendApplicationAcknowledgement par sendAppMail. Vérifier RESEND_API_KEY en prod.

[MEDIUM] (notifs_mails_killswitch) Le kill-switch journalise les envois bloqués en status='failed' → l'auto-diagnostic les compte comme « mails en échec » et déclenche de faux incidents
  FICHIER: C:/Users/KElba/Documents/GitHub/Formulaire_candidats/caftan-rh/src/lib/outbound-guard.ts:60-70
  POURQUOI: reportBlockedOutbound() écrit chaque envoi coupé par le kill-switch dans outbound_mails avec status:'failed'. Or runHealthChecks() (src/lib/system/health-checks.ts:90-102) compte TOUT outbound_mails status='failed' sur 7j et lève un incident warning « X mail(s) en échec — des destinataires n'ont rien reçu », consommé par l'agent d'astreinte/incident-manager. Chaque relance signature J+2/5/7 et chaque rappel entretien bloqués (comportement NORMAL et voulu) gonflent ce compteur -> alertes incidents récurrentes et fausses, à l'opposé de l'objectif « <1h/semaine RH ». Les crons (signature-reminder.ts:42) prennent soin de ne PAS compter 'blocked' en erreur, mais le health-check lit le journal brut et les compte.
  FIX: Distinguer les blocages du kill-switch des vraies pannes : soit logguer les blocages avec un status/flag dédié (ex. status='blocked' ou delivery_provider='none' + source 'auto-outbound-blocked'), soit exclure explicitement ces lignes du compteur failed_mails (WHERE source <> 'auto-outbound-blocked' AND delivery_provider <> 'none').

[MEDIUM] (notifs_mails_killswitch) interview-reminder ne distingue que 'blocked' : un envoi 'skipped'/'failed' (Resend absent) est compté comme candidat relancé et journalisé 'outbound'
  FICHIER: C:/Users/KElba/Documents/GitHub/Formulaire_candidats/caftan-rh/src/app/api/cron/interview-reminder/route.ts:97-116
  POURQUOI: Le cron ne saute la comptabilisation/journalisation que si sr.blocked est vrai. sendInterviewReminder (emails.ts/Resend) renvoie {skipped:true} quand RESEND_API_KEY manque et {error} sur échec Resend — dans ces deux cas non-'blocked', le cron fait reminded++ ET insère une ligne messages direction='outbound' « Rappel automatique : entretien demain », alors qu'AUCUN mail n'est parti (outbound_mails, lui, logge 'failed'). Incohérence entre messages (candidat « contacté ») et outbound_mails (échec) : la RH croit le candidat rappelé.
  FIX: Ne compter reminded++ et n'insérer la ligne messages que si l'envoi a réellement réussi (retour {ok:true}); traiter skipped/error comme non-envoyé (ni compté, ni journalisé 'contacté'), à l'image du traitement de 'blocked'.

[MEDIUM] (entites_paie_contrat) Avance salariale perdue quand une fiche plus élevée arrive après une plus petite pour le même mois
  FICHIER: src/lib/payslip-processor.ts:176
  POURQUOI: Quand une 2e fiche du même mois a un net >= à la fiche existante (branche ligne 171 `else`), l'existante (potentiellement l'ancienne PRINCIPALE qui a déjà déduit l'avance et remis employees.salary_advance_amount à 0 via ligne 388-392) est rebasculée en secondaire avec advance_deducted=0 et amount_to_pay=otherNet (ligne 180-191). La nouvelle principale lit alors `advance = salary_advance_amount` = 0 (déjà consommée) et déduit 0 (ligne 197-198). Résultat : l'avance n'est déduite d'AUCUNE des deux fiches — le travailleur est payé net_A + net_B sans déduction, l'entreprise ne récupère jamais l'avance déjà versée. Invariant paie violé (l'avance doit être déduite d'exactement une fiche principale).
  FIX: Lors du basculement de l'ancienne principale en secondaire, restaurer son avance sur l'employé (ou reporter advance_deducted sur la nouvelle principale) avant de recalculer amount_to_pay, pour garantir qu'une avance consommée reste déduite exactement une fois.

[LOW] (auth_i18n_session) Messages d'erreur des actions candidat/employé non traduits (hardcodés en français) pour les utilisateurs NL
  FICHIER: src/app/me/screening/actions.ts:19
  POURQUOI: Les server actions de l'espace utilisateur final (bilingue FR/NL imposé par CLAUDE.md) renvoient des erreurs en dur en français affichées telles quelles à l'utilisateur : ex. 'Non authentifié', 'Profil candidat introuvable', 'Aucun questionnaire actif' (me/screening/actions.ts:19,23,32), 'Aucune fiche employé liée à ce compte' (me/contract-info/actions.ts:31), 'Aucune fiche employé' (me/expenses/actions.ts:25). Un candidat/employé en locale NL voit des messages FR, ce qui casse la promesse bilingue et peut dérouter en situation d'erreur.
  FIX: Renvoyer des codes d'erreur stables (ex. 'not_authenticated','no_employee') et les mapper vers t(key, locale) côté client, ou passer la locale à l'action pour renvoyer la string déjà traduite via le dictionnaire i18n existant.

[LOW] (planning_shifts) Les garde-fous 'aujourd'hui/maintenant' du solver utilisent l'heure serveur UTC au lieu d'Europe/Brussels
  FICHIER: src/app/planning/sites/[code]/actions.ts:658 ; src/app/planning/employees/[id]/generate-actions.ts:121
  POURQUOI: Le solver calcule todayISO = toISODate(new Date()) et nowMin = new Date().getHours()*60+getMinutes(), puis skippe les créneaux via needStartInPast() et via `if (dateISO < tomorrowISO) continue`. src/lib/datetime.ts documente explicitement que Vercel tourne en UTC et fournit des helpers Brussels (APP_TZ, brusselsWallTimeToUtc) — mais le solver ne les utilise pas. En prod (UTC), nowMin est 1 à 2h en retard sur l'heure belge : la marge NOW_GUARD de 30 min est faussée et des shifts dont le début belge est déjà passé peuvent être générés (guard trop laxiste). Pire, autour de minuit belge (UTC = veille), toISODate(new Date()) renvoie la veille, donc tomorrowISO/todayISO glissent d'un jour et le filtre 'pas de génération dans le passé' laisse passer ou bloque le mauvais jour. C'est le pattern 'fuseau pointage' déjà connu, ici sur la génération de planning.
  FIX: Remplacer new Date()/getHours()/toISODate(new Date()) par une date/heure murale Europe/Brussels (via un helper basé sur Intl.DateTimeFormat timeZone: 'Europe/Brussels', cf. brusselsOffsetMinutes dans datetime.ts) pour todayISO, tomorrowISO et nowMin, dans les deux générateurs.

[LOW] (planning_shifts) Fenêtre été/hiver de la pause prière codée en dur au 1er avril–30 sept au lieu des vraies dates de changement d'heure
  FICHIER: src/lib/auto-planning.ts:22
  POURQUOI: DEFAULT_PRAYER_PAUSE fixe dstStart='04-01' et dstEnd='10-01', et isSummerDate() bascule été/hiver sur cette base. Le passage réel à l'heure d'été/hiver en Belgique est le dernier dimanche de mars (2026: 29 mars) et le dernier dimanche d'octobre (2026: 25 oct). Pour les shifts du vendredi entre ces bornes et le 1er avril / 1er octobre, la mauvaise plage horaire de prière (hiver au lieu d'été, décalage ~1h) est appliquée, soit un créneau de pause faux plusieurs semaines par an. Impact limité et les valeurs sont surchargées par org_settings, d'où la sévérité basse.
  FIX: Calculer dynamiquement le DST belge (dernier dimanche de mars / octobre) ou aligner les valeurs par défaut, plutôt qu'un intervalle MM-DD fixe.

[LOW] (pointage_clock) Géofence contournable : coordonnées 100% côté client, précision jamais vérifiée, et site sans coords désactive le contrôle en silence
  FICHIER: src/app/me/clock/actions.ts:20-30
  POURQUOI: checkGeofence renvoie ok:true (aucune anomalie) dès que le site n'a pas de lat/lng (ligne 20-22), donc en mode strict un magasin mal configuré laisse pointer de n'importe où sans flag is_anomalous. De plus geo_accuracy_m est capturé mais jamais utilisé pour rejeter : une position falsifiée dans le navigateur (accuracy 0 ou énorme) passe le rayon sans contrôle. Le clock-OUT (clockOutAction:245-294) n'applique aucune géofence. Résultat : le contrôle de présence physique promis est facilement contournable et l'incohérence de config n'est pas signalée.
  FIX: Flag is_anomalous quand geofenceStrict est actif mais le site n'a pas de coords (au lieu de ok:true silencieux) ; rejeter/flagger les positions à accuracy > seuil (ex. >200m) ; documenter/journaliser les OUT hors zone.

[LOW] (pointage_clock) Groupement des journées par date UTC (slice(0,10)) → sessions de soirée coupées et clés d'auto-OUT faussées
  FICHIER: src/app/api/cron/tuya-auto-out/route.ts:63-67
  POURQUOI: inKey et le filtre same-day utilisent occurred_at.slice(0,10) = date UTC (aussi tuya-poll.ts:130,201 et force-close-orphans:74 avec `${day}T23:59:59Z`). En été (UTC+2) un pointage après 22:00 local bascule au lendemain UTC ; un IN à 23:30 local et son OUT à 00:30 local tombent sur deux dates UTC différentes. La déduplication par (employee|noshift|jour), la détection 'OUT le même jour après IN' et le clamp de fin de journée se trompent alors de journée → auto-OUT indus ou orphelins non fermés pour les shifts de fin de journée.
  FIX: Dériver le 'jour métier' en Europe/Brussels (via datetime.ts) partout où slice(0,10) sert de clé de journée, plutôt que la date UTC brute.

[LOW] (pointage_clock) Erreur brute de la base (trigger anti-double-pointage) renvoyée telle quelle au travailleur
  FICHIER: src/app/me/clock/actions.ts:226
  POURQUOI: clockInAction insère sans vérifier au préalable une session déjà ouverte et se repose sur le trigger prevent_double_clock_in. En cas de rejet, `return { error: error.message }` remonte un message Postgres cru (ex. 'new row violates check constraint ...') affiché dans un toast à l'employé, illisible et non bilingue FR/NL. Idem clockOutAction:295.
  FIX: Intercepter les codes d'erreur du trigger (double clock-in) et renvoyer un message métier traduit ('Tu es déjà pointé' / 'Je bent al ingeklokt'), en gardant le message brut uniquement dans les logs.

[LOW] (conges_absences_swaps) Couverture d'absence et swaps ignorent les indisponibilités déclarées et les jours off fixes
  FICHIER: src/app/me/swaps/actions.ts:66
  POURQUOI: evaluateSwap (66-224) et volunteerForAbsenceAction (src/app/me/absence/actions.ts:255-295) ne consultent jamais employee_unavailabilities ni employees.fixed_off_days. Un collègue peut donc être auto-validé pour couvrir une absence ou récupérer un shift sur un créneau qu'il a explicitement déclaré indisponible, ou sur son jour de repos fixe. Cela vide de sens le module 'dispos déclarées' et peut placer quelqu'un en infraction avec ses contraintes (garde d'enfant, cours, etc.), donc générer des non-présentations.
  FIX: Ajouter aux checks d'auto-validation (swap + volontariat absence) une vérification que le créneau ne chevauche aucune employee_unavailabilities (récurrente day_of_week ou date_specific) et ne tombe pas sur fixed_off_days ; escalader au manager sinon.

[LOW] (conges_absences_swaps) Condition de course sur la couverture d'absence et l'acceptation de swap (double transfert)
  FICHIER: src/app/me/absence/actions.ts:230
  POURQUOI: volunteerForAbsenceAction lit le statut ('open') puis met à jour shifts.employee_id et l'absence de façon non atomique (230-305). Deux volontaires simultanés passent tous deux le garde 'covered/resolved', transfèrent le shift l'un après l'autre (le dernier gagne) et reçoivent tous deux une confirmation 'tu couvres', alors qu'un seul détient réellement le shift. Même schéma TOCTOU dans acceptSwapAction. Risque de malentendu sur qui est réellement de service.
  FIX: Rendre la transition atomique : UPDATE conditionnel sur le statut (WHERE status='open') et n'appliquer le transfert que si l'update a affecté 1 ligne ; sinon renvoyer 'déjà couverte'.

[LOW] (conges_absences_swaps) Le pic d'absentéisme double-compte le demandeur
  FICHIER: src/lib/leave-auto-validation.ts:328
  POURQUOI: computeMaxAbsentsPct ajoute includeSelf=+1 pour l'employé courant chaque jour, alors que `offs` inclut déjà les autres demandes approved/pending du même employé (seul excludeRequestId est retiré, ligne 318). Si l'employé a une autre demande qui chevauche, il est compté deux fois, gonflant le % d'absents et pouvant faire échouer à tort l'auto-validation ('too_many_absents'), donc générer une escalade manager inutile. Même logique dans coverage-engine.ts:196.
  FIX: Exclure toutes les lignes de l'employé courant de `offs` avant d'ajouter includeSelf, ou ne pas ajouter includeSelf si une ligne de l'employé chevauche déjà le jour évalué.

[LOW] (embauche_candidat) Choix RH 'CDI' silencieusement converti en CDD sans terme (end_date null)
  FICHIER: src/app/rh/candidates/[id]/hire-actions.ts:76
  POURQUOI: Le dialog d'embauche propose 'CDI' (hire-button.tsx:41). Pour un CDI, hire-actions force end_date=null (ligne 76). Puis le trigger normalize_employee_legal (legal_rules_engine.sql:53-56, regle no_cdi_policy activee par defaut) reecrit silencieusement contract_type CDI -> CDD tout en gardant end_date null. On obtient un 'CDD sans date de fin', juridiquement incoherent, et le choix explicite de la RH est ecrase sans aucun retour a l'ecran (le trigger emet seulement un RAISE WARNING invisible cote app). De plus l'UPDATE employees de hire-actions.ts:176-186 n'a aucun controle d'erreur, donc un rejet eventuel (CHECK contract_type sur 'Intérim'/'Freelance') passerait inapercu.
  FIX: Si la politique no-CDI est voulue, retirer 'CDI' de la liste du dialog (ou avertir la RH que le CDI sera converti) et forcer un end_date par defaut lors de la conversion en CDD ; ajouter la verification d'erreur sur l'UPDATE employees et remonter tout echec dans les `steps`.

[LOW] (embauche_candidat) Les liens contract-info (pre-embauche) n'expirent jamais et restent editables apres soumission
  FICHIER: src/app/contract-info/[token]/actions.ts:87
  POURQUOI: submitContractInfoAction pose bien `completed_at`, mais ni la page (src/app/contract-info/[token]/page.tsx:61-121) ni autosaveContractInfoAction ne verifient jamais completed_at ou une quelconque expiration : le token reste valide et modifiable indefiniment. Un lien de pre-embauche fuite (WhatsApp, mail transfere) permet a n'importe qui de continuer a lire/ecrire des donnees personnelles sensibles du candidat (IBAN, NISS, date de naissance, adresse) sans authentification, sans limite de temps. Probleme RGPD (minimisation/duree de conservation) sur des donnees d'identite.
  FIX: Ajouter une expiration (ex. valid_until) et/ou bloquer l'ecriture apres completed_at ; verifier ces conditions dans page.tsx et dans autosave/submitContractInfoAction, et afficher InvalidShell si le lien est expire/deja finalise.

[LOW] (notifs_mails_killswitch) scheduleInterviewAction journalise une « Confirmation d'entretien » envoyée au candidat même quand l'envoi a échoué (fausse trace RH)
  FICHIER: C:/Users/KElba/Documents/GitHub/Formulaire_candidats/caftan-rh/src/app/rh/actions.ts:144-164
  POURQUOI: L'appel sendInterviewConfirmation est dans un try/catch best-effort (144-157), mais l'INSERT dans messages direction='outbound' « Confirmation d'entretien » (158-164) est HORS du catch et inconditionnel. Comme l'envoi passe par emails.ts/Resend (désactivé, cf. finding précédent), le candidat ne reçoit rien mais la timeline RH affiche « Confirmation d'entretien » comme envoyée. La RH croit le candidat prévenu -> no-show / décision faussée.
  FIX: N'insérer la ligne messages 'outbound' que si l'envoi a réellement réussi (vérifier le retour {ok:true} et l'absence de {skipped|blocked|error}), sinon logguer un statut interne « non envoyé » ou notifier la RH pour envoi manuel.

[LOW] (notifs_mails_killswitch) Le cron pre-interview-reminder régénère le token du pré-entretien (invalidant le lien encore valide du candidat) sans jamais lui envoyer le nouveau lien
  FICHIER: C:/Users/KElba/Documents/GitHub/Formulaire_candidats/caftan-rh/src/app/api/cron/pre-interview-reminder/route.ts:181-273
  POURQUOI: Pour tout pré-entretien expirant sous 2 jours, le cron remplace token par un newToken (181-189), ce qui rend IMMÉDIATEMENT mort le lien que le candidat détenait encore (l'URL publique dérive du token). Mais il n'envoie AUCUN mail au candidat : il insère seulement un brouillon dans messages + une activité + une notif RH. Le nouveau lien n'est donc jamais transmis au candidat. Un candidat qui avait un lien valide se retrouve avec un lien 404 et sans remplacement — flux pré-entretien cassé en silence, dépendant d'une action manuelle RH qui n'est pas garantie.
  FIX: Ne pas rotationner le token tant qu'un canal de délivrance n'existe pas : soit prolonger expires_at SANS changer le token (le lien existant reste valide), soit générer le nouveau token uniquement au moment d'un renvoi manuel RH depuis la fiche candidat.

[LOW] (notifs_mails_killswitch) Le trigger DB de notif au destinataire d'un mail utilise un lien générique '/me' (interdit par la règle lien-direct de l'app)
  FICHIER: C:/Users/KElba/Documents/GitHub/Formulaire_candidats/caftan-rh/supabase/migrations/20260620000732_notif_triggers_comm_channels.sql:182-196
  POURQUOI: notify_recipient_on_outbound_mail() insère la notification (et donc, via trg_notify_push_after_insert, le push web) avec link='/me' en dur. Or le helper applicatif notify.ts:36-54 (assertLink) REJETTE explicitement '/me' comme lien générique interdit — le trigger contourne cette règle. Le clic sur la notif/push du travailleur atterrit sur le dashboard générique au lieu du mail concerné (la vue existe pourtant : /me/mails). Contredit la règle « toute notif = objet précis + lien direct ».
  FIX: Remplacer link '/me' par '/me/mails' (ou un lien profond vers le mail précis via new.id) dans la fonction notify_recipient_on_outbound_mail, comme le fait notify() côté app.

[LOW] (data_integrity_rls) Le trigger de promotion candidat→employé ne recopie pas les champs transport ni le statut étudiant
  FICHIER: supabase/migrations/20260620000590_promote_copy_identity.sql:15-34
  POURQUOI: promote_application_to_employee() copie l'identité/banque (iban, bic, nrn, adresse...) mais PAS transport_type / transport_frequency / transport_price ni is_student, colonnes ajoutées sur candidates par la migration 20260620001020 (flux candidat pré-validé /contract-info) et qui existent aussi sur employees (écrites par saveEmployeeAdminAction). Un candidat qui déclare son mode de transport et son statut étudiant via le lien de pré-embauche voit ces données PERDUES à l'embauche : remboursement transport non initialisé et statut étudiant (impactant Dimona worker_type STU/OTH et le calcul légal) non transféré. worker_field_submissions n'est pas repris non plus, faisant perdre l'horodatage self-service.
  FIX: Ajouter transport_type, transport_frequency, transport_price, is_student (et worker_field_submissions) à la liste INSERT du trigger, en les prenant depuis cand.*. Vérifier aussi le fallback d'insertion directe dans hire-actions.ts:148-164 qui omet iban/nrn/adresse (dégradé si trigger absent).

[LOW] (data_integrity_rls) Les tokens contract-info n'expirent jamais et restent réutilisables après complétion (overwrite permanent des données d'embauche)
  FICHIER: src/app/contract-info/[token]/actions.ts:13-27
  POURQUOI: resolveTarget() ne lit que id/employee_id/candidate_id et n'applique AUCUN contrôle d'expiration ni de single-use : ni expires_at (colonne inexistante partout), ni completed_at (la page page.tsx:66 le lit mais les actions submit/autosave l'ignorent). submitContractInfoAction pose completed_at (ligne 87) mais un token déjà complété reste pleinement exploitable pour ré-écrire, via le client service-role (bypass RLS), l'IBAN/NRN/adresse/date de naissance d'un employé ou candidat. Un lien de pré-embauche fuité (mail transféré, historique) permet donc de modifier indéfiniment des données RH sensibles. Le message d'erreur 'Lien invalide ou expiré' est trompeur car aucune expiration n'est réellement vérifiée.
  FIX: Ajouter une colonne expires_at (ex. +14 jours) et vérifier dans resolveTarget que le token n'est ni expiré ni déjà completed_at (au moins refuser submit après complétion, ou régénérer/invalider). Rejeter avec une vraie logique d'expiration.

[LOW] (data_integrity_rls) clockIn ne vérifie pas un pointage déjà ouvert : double IN consécutif corrompt le calcul d'heures
  FICHIER: src/app/me/clock/actions.ts:210-225
  POURQUOI: clockInAction insère toujours une entrée kind='in' sans vérifier que la dernière entrée de l'employé est bien un 'out'. clockOutAction, lui, contrôle que la dernière entrée est un 'in' (ligne 267). Asymétrie : un double-tap ou un re-clock crée deux 'in' consécutifs sans 'out' intermédiaire. Le clockOut suivant se pariera avec le dernier 'in', laissant le premier 'in' orphelin ; le calcul de durée (occurredAt - last.occurred_at) et les vues de présence deviennent faux. Sur une plateforme où les heures pilotent le régime horaire (work_time_kind) et la paie, cela fausse une donnée RH décisionnelle.
  FIX: Avant insertion, charger la dernière clock_entry de l'employé ; si kind='in' (pointage déjà ouvert), renvoyer une erreur explicite (ou ignorer idempotent) au lieu d'insérer un second 'in'.

[LOW] (data_integrity_rls) Fusion de doublons candidats : flag 'nullable' inutilisé et transferts FK non protégés contre les conflits d'unicité / perte de champs
  FICHIER: src/app/admin/candidates/duplicates/actions.ts:23-92
  POURQUOI: Le type FkTransfer définit un flag 'nullable' (skip/set-null si conflit, cf. commentaire lignes 15-19) mais la boucle de transfert (lignes 61-79) fait un UPDATE inconditionnel pour TOUTES les tables, ignorant ce flag. Si deux candidats en doublon ont chacun une ligne employees (ou une FK sous contrainte unique), transférer les deux vers keeperId crée deux lignes avec le même candidate_id (ou fait échouer l'update, marqué -1 et poursuivi) : soit doublon de FK, soit orphelin silencieux. De plus, la fusion ne fait AUCUN merge au niveau champ : le keeper garde ses valeurs et les champs présents uniquement sur un duplicate (ex. iban rempli sur le doublon mais vide sur le keeper) sont définitivement perdus au DELETE (ligne 82-85), contredisant le commentaire 'keeper = candidate avec le plus de données'.
  FIX: Avant DELETE, coalescer champ par champ les données non-null des duplicates vers le keeper (sans écraser une valeur existante). Implémenter réellement la sémantique 'nullable' (gérer les conflits d'unicité par set-null ou skip) ou envelopper la fusion dans une fonction SQL transactionnelle atomique.

[LOW] (entites_paie_contrat) Seuil temps plein/partiel incohérent selon le chemin (30h / 36h / 38h) : le contrat rendu ne correspond pas à ce que l'opérateur voit
  FICHIER: src/app/planning/employees/[id]/sign-contract-button.tsx:92
  POURQUOI: Le seuil full/part diffère selon le code : deriveTemplateCode utilise `>= 30 -> plein` (sign-contract-button.tsx:92), rehire-actions.ts:26 utilise `>= 36 -> full`, le trigger DB normalize_employee_legal utilise `>= 30 -> 'full'` (20260620000610:59), tandis que contract-render-inputs.ts:100, align-profile-actions.ts:115 et contract-terms-actions.ts:64 utilisent `< 38 -> partiel`. Conséquence concrète : pour un travailleur à 30–37h, le bouton affiche « CDD temps plein » et envoie templateCode='employee', mais le serveur (resolveContractRenderInputs, source de vérité) redérive à < 38 et rend un contrat employee_pt (temps partiel). L'opérateur signe/envoie en croyant un temps plein alors que le document généré est un temps partiel — décision RH faussée et régime horaire incohérent selon le point d'entrée.
  FIX: Centraliser le seuil dans une constante unique (38h, standard CP 201 belge) et l'importer partout : sign-contract-button.tsx:92, rehire-actions.ts:26 et le trigger SQL. Supprimer les seuils 30 et 36 divergents.

[LOW] (entites_paie_contrat) Mauvaise entité employeur sur un document signé (rupture/contrat) faute d'ordre déterministe et repli AMD
  FICHIER: src/lib/employer-orgs.ts:72
  POURQUOI: resolveEmployerOrgForEmployee sert à déterminer l'entité qui signe la lettre de rupture (termination-actions.ts:118,643 ; api/terminations/[id]/letter/route.ts:101) et l'entité par défaut du contrat (page.tsx:126). Le repli pickKey(false) sélectionne « n'importe quelle affectation active » via `.limit(1).maybeSingle()` SANS ORDER BY (ligne 73-80) : pour un travailleur ayant des affectations actives sur des sites de deux entités différentes, l'entité retenue est arbitraire (dépend de l'ordre non garanti de Postgres) → mauvaise entité juridique sur un document signé. En dernier recours il retombe sur amd_megastore (ligne 99), ce qui peut attribuer à AMD un travailleur relevant réellement de Homix/Caftan (ex. site sans employer_org_key). Documents légaux (rupture) potentiellement émis au nom de la mauvaise société.
  FIX: Ajouter un ORDER BY déterministe (ex. is_primary desc, start_date desc) au pickKey de repli, et signaler explicitement (au lieu de retomber silencieusement sur amd_megastore) quand aucune entité fiable n'est résolue, pour que l'opérateur choisisse.

[LOW] (entites_paie_contrat) amount_to_pay d'une fiche payée réécrit lors d'un ré-import (incohérence net = avance + à payer)
  FICHIER: src/lib/payslip-processor.ts:279
  POURQUOI: Dans le chemin dédup, `newAmountToPay = preservedPaid ? amountToPay : Math.max(0, net - preservedAdvance)` (ligne 279-281). Pour une fiche déjà PAYÉE, il écrit amountToPay (recalculé sur l'avance COURANTE, souvent 0 car consommée) au lieu de préserver le montant réellement payé — le commentaire dit pourtant « garde tel quel ». advance_deducted est préservé (hors payload) mais amount_to_pay devient net, si bien que advance_deducted + amount_to_pay ≠ net et ≠ paid_amount. Incohérence de reporting/audit sur les fiches payées ré-importées (pas de paiement erroné car QR mis à null pour les payées).
  FIX: Pour preservedPaid, conserver l'ancien amount_to_pay (ne pas l'inclure dans le payload d'UPDATE) plutôt que de le recalculer, afin de préserver l'invariant net = advance_deducted + amount_to_pay.

[LOW] (mobile_pwa_theme) Aucun color-scheme: light global — l'app staff (RH/planning/admin) reste vulnérable au mode sombre iOS/Android
  FICHIER: src/app/layout.tsx:46-58 (viewport + <html>) et src/app/globals.css:36-45
  POURQUOI: La règle dure du projet est "clair uniquement, jamais de dark:, casse la lisibilité mobile en mode sombre iPhone". Les développeurs ont verrouillé color-scheme:light UNIQUEMENT sur les surfaces publiques/mobiles (src/app/candidat/layout.tsx:9, src/app/m/layout.tsx:40, src/app/m/mobile-dashboard.tsx:101, contract-info/[token]/page.tsx:33, renewal/[token]/page.tsx:9, candidat/login/page.tsx:14). Le root <html> et globals.css n'ont AUCUN color-scheme. Résultat : sur iPhone/Android en mode sombre, toute l'app STAFF (app-shell + toutes les pages /rh, /planning, /admin, /me, /chat, formulaires d'embauche, contrats, congés) rend les contrôles natifs (input[type=date], caret, autofill, cases à cocher, scrollbars, popover calendrier, champs sans bg explicite) avec le chrome UA sombre — texte/fond sombre-sur-sombre. C'est exactement la surface la plus utilisée au quotidien pour saisir/lire des données RH sur mobile, et c'est le trou laissé par les garde-fous locaux. La présence de ces garde-fous éparpillés prouve que le bug de mode sombre est connu et réel ; seule l'app principale a été oubliée.
  FIX: Forcer le rendu clair globalement une seule fois au lieu de patcher page par page : ajouter `color-scheme: light` sur `html, body` dans globals.css (ou `colorScheme: 'light'` dans l'export `viewport` de layout.tsx / attribut style sur <html>). Cela couvre toutes les pages staff et rend les colorScheme:light locaux redondants.

[LOW] (mobile_pwa_theme) La bannière d'activation des notifications (mode "activate") recouvre la barre d'onglets mobile et bloque la navigation
  FICHIER: src/components/push-activation-banner.tsx:213
  POURQUOI: Le mode "activate" est positionné `fixed bottom-0 left-0 right-0 z-40` (pleine largeur sur mobile, le breakpoint sm: ne s'applique qu'à partir de tablette). La barre d'onglets mobile (src/components/mobile-tab-bar.tsx:39) est aussi `fixed bottom-0 inset-x-0 z-40` avec des cibles de 56px. Même z-index, mais la bannière est rendue APRÈS les children dans le root layout (layout.tsx) donc elle passe au-dessus et masque la barre de navigation du bas (Accueil/Planning/Pointage/Plus). Sur iPhone en PWA — la cible produit — l'utilisateur perd l'accès à la navigation principale tant qu'il n'a pas fermé la bannière. Le mode "ios-pwa" juste au-dessus (ligne 175) gère correctement le cas avec `bottom-[calc(64px+env(safe-area-inset-bottom))]` ; seul le mode "activate" a été oublié — incohérence interne au même composant.
  FIX: Aligner le mode "activate" sur le mode "ios-pwa" : remplacer `bottom-0 ... pb-safe` par `bottom-[calc(64px+env(safe-area-inset-bottom))]` sur mobile (garder `sm:bottom-4`) pour que la bannière flotte au-dessus de la tab bar sans la couvrir.

[LOW] (mobile_pwa_theme) La bannière d'installation PWA chevauche la barre d'onglets mobile (bas de carte masqué)
  FICHIER: src/components/install-prompt.tsx:104 et :138
  POURQUOI: Les deux variantes du prompt d'installation sont `fixed bottom-3 ... z-20`. En navigateur mobile (cas où le prompt s'affiche, non-standalone) l'app-shell affiche toujours la barre d'onglets `fixed bottom-0 z-40` (mobile-tab-bar.tsx:39, 56px de haut). À bottom-3 (12px) la carte du prompt entre dans la zone des 56px de la barre ; comme la barre est z-40 > z-20, le bas de la carte d'installation passe derrière la navigation et devient illisible/partiellement masqué. Moins grave que la bannière push (n'empêche pas la navigation) mais dégrade l'UX d'onboarding mobile.
  FIX: Remonter le prompt au-dessus de la tab bar sur mobile : utiliser `bottom-[calc(64px+env(safe-area-inset-bottom))]` (avec `sm:bottom-3`) comme pour la bannière push corrigée, afin qu'il ne recouvre plus la barre d'onglets.

[LOW] (crons_background) Le cron payslip-secondary-notify n'est jamais planifie -> secondes fiches de paie differees jamais debloquees ni payees
  FICHIER: .github/workflows/caftan-crons.yml:76-99
  POURQUOI: Le route src/app/api/cron/payslip-secondary-notify/route.ts fait passer les fiches secondaires (double fiche meme mois, is_secondary=true) de payment_status 'scheduled' a 'pending' a J+6 et notifie la RH. La FAQ (src/lib/faq-content.ts:65) le documente explicitement comme 'Cron quotidien qui libere automatiquement a J+6'. Or il n'apparait NI dans on.schedule, NI dans le mapping MAP du workflow GitHub Actions, NI dans vercel-crons-backup.json, et aucun autre route/trigger ne fait ce passage scheduled->pending (verifie: seul ce fichier contient la logique). Consequence: les secondes fiches restent bloquees 'scheduled' indefiniment, ne sont jamais debloquees pour paiement et la RH n'est jamais alertee -> un travailleur ne recoit pas sa regularisation de salaire.
  FIX: Ajouter payslip-secondary-notify a un schedule quotidien du workflow (ex. l'entree '0 8 * * *' ou une nouvelle '0 6 * * *') et a la MAP correspondante, comme ce qui a ete fait en greffant employment-lifecycle dans cdd-renewal-scan. Alternative: greffer la meme logique de deblocage dans un cron deja planifie (ex. payslip-imap-poll).

[LOW] (crons_background) signature-reminder : garde d'auth fail-open (endpoint public si CRON_SECRET absent), incoherent avec les autres crons
  FICHIER: caftan-rh/src/app/api/cron/signature-reminder/route.ts:52
  POURQUOI: La condition 'if (process.env.CRON_SECRET && authHeader !== ...)' ne rejette QUE si CRON_SECRET est defini. Si la variable est absente/vide, le check est saute et l'endpoint devient public — alors que tous les autres routes cron utilisent le pattern fail-closed 'if (!secret || header !== ...)' (ex. anomaly-scan:17, pre-interview-reminder:55). Or signature-reminder declenche des envois outbound (relances de signature contrat/rupture); un endpoint ouvert permettrait de le declencher sans secret. En prod CRON_SECRET est present, donc impact limite, mais l'incoherence est un vrai trou defensif.
  FIX: Aligner sur le pattern fail-closed: 'const secret = process.env.CRON_SECRET; if (!secret || authHeader !== `Bearer ${secret}`) return 401;'.
