# Audit chaine jusqu-a-signature

[HIGH] (hire_path) Chaine ROMPUE : un candidat pre-valide (prevalidated=true, sans candidature) ne peut jamais etre embauche
  FICHIER: C:/Users/KElba/Documents/GitHub/Formulaire_candidats/caftan-rh/src/app/rh/candidates/prevalidated/[id]/page.tsx
  POURQUOI: createPrevalidatedCandidateAction (prevalidated-actions.ts:21-27) insere UNIQUEMENT une row dans `candidates` + un `contract_info_tokens` : aucune `applications` n'est creee. Or tout le flux d'embauche exige un applicationId : hireCandidateAction(applicationId, ...) charge la candidature (hire-actions.ts:89-97, retourne 'Candidature introuvable' sinon) et s'appuie sur le trigger promote_application_to_employee() qui ne se declenche que sur applications.status='hired'. Le seul bouton qui appelle hireCandidateAction (HireCandidateButton) n'est monte QUE sur /rh/candidates/[id]/page.tsx (base sur applications). La page de revue du pre-valide (prevalidated/[id]/page.tsx) n'affiche AUCUN bouton 'Embaucher' — elle ne fait qu'afficher les infos, la carte d'identite et les indispos. Quand le candidat termine son dossier, la notif RH pointe vers cette page cul-de-sac (contract-info/[token]/actions.ts:95). Consequence : toutes les infos secretariat social auto-remplies (IBAN, NISS, adresse, naissance, statut etudiant, transport) et toutes les candidate_unavailabilities declarees sont une impasse totale — rien ne les convertit en fiche employee, et le 'rejeu des indispos' de hire-actions.ts:189-228 (qui lit pourtant candidate_unavailabilities) n'est jamais atteint pour ce type de candidat. Le flux 'candidat pre-valide -> employe -> contrat -> signature' est integralement casse des la premiere marche.
  FIX: Ajouter sur /rh/candidates/prevalidated/[id] un bouton d'embauche. Deux options : (a) creer une variante de hireCandidateAction prenant un candidateId qui insere directement l'employee depuis `candidates` (copie identite + is_student + transport + rejeu indispos + prepareContract + Dimona), ou (b) generer une `applications` 'shell' pour ce candidat afin de rebrancher le trigger existant, puis reutiliser hireCandidateAction. Router la notif de fin de dossier vers cette action 1-clic.

[HIGH] (contract_gen) saveContractTermsAction écrit 'partial' alors que le CHECK exige 'part' → toute correction d'un temps partiel est rejetée en bloc
  FICHIER: caftan-rh/src/app/planning/employees/[id]/contract-terms-actions.ts (lignes 46-53 et 63-65) + select value="partial" dans sign-contract-button.tsx ligne 342
  POURQUOI: Le popup 'Récapitulatif du contrat' enregistre les paramètres via saveContractTermsAction. La validation accepte 'partial' (ligne 48: `wk !== 'full' && wk !== 'partial'`) et l'écrit tel quel (ligne 51), et la dérivation implicite ligne 64 écrit `h < 38 ? 'partial' : 'full'`. Or la contrainte CHECK de employees.work_time_kind exige 'part'/'full' (confirmé par le commentaire de la migration 20260703002000_fix_normalize_employee_legal_hours.sql ligne 4 et par le trigger qui écrit 'part'). Le trigger normalize_employee_legal ne coerce PAS 'partial'→'part' (il ne dérive que si NULL). Résultat: dès qu'un opérateur enregistre un régime 'Temps partiel' OU corrige les heures < 38h sans toucher au régime, l'UPDATE employees viole le CHECK. saveContractTermsAction renvoie {error: updateErr.message} et TOUT le updatePayload (heures, dates, poste, taux, contrat) est rejeté ensemble. Impossible de préparer/corriger un contrat temps partiel depuis ce popup.
  FIX: Normaliser vers la valeur canonique 'part' partout: dans contract-terms-actions.ts remplacer les deux occurrences 'partial' par 'part' (et accepter 'part' dans la validation, en tolérant 'partial' en entrée mais en le mappant sur 'part'); dans sign-contract-button.tsx mettre <option value="part"> (et normaliser l'état initial terms.work_time_kind qui lit employeeRecord pouvant contenir 'partial').

[HIGH] (esign) Alignement fiche<->contrat écrit work_time_kind='partial' (interdit par le CHECK 'part') → l'UPDATE échoue en silence et l'audit log MENT
  FICHIER: src/app/planning/employees/[id]/sign-contract-actions.ts (l.462-484)
  POURQUOI: Quand l'opérateur valide les discordances (acceptDiscrepancies), le patch d'alignement fait `patch.work_time_kind = effTpl === 'employee_pt' ? 'partial' : 'full'` (l.471). Or la contrainte CHECK de employees exige 'part' (jamais 'partial') — confirmé par la migration 20260703002000_fix_normalize_employee_legal_hours.sql qui corrige exactement ce bug ailleurs. Comme tous les champs sont écrits en UN SEUL update (l.474) et que l'erreur retournée n'est pas vérifiée, la moindre présence de work_time_kind dans les discordances fait échouer TOUT l'update (heures, contract_type, job_title, hourly_rate, dates ne sont PAS alignés non plus), mais le code enchaîne et insère quand même un activity_log « Fiche alignée sur le contrat » (l.476-482). Faux enregistrement d'audit + fiche restée divergente (base Dimona/paie) = violation de la règle « signaler les discordances, jamais en silence ».
  FIX: Remplacer 'partial' par 'part'. Et vérifier l'erreur de l'update avant de logguer l'alignement : `const { error } = await admin.from('employees').update(patch)...; if (error) { log échec + ne pas écrire 'Fiche alignée' }`.

[HIGH] (post_sign) dimona_declarations: split-brain schema — la moitié du flux Dimona IN plante en prod
  FICHIER: caftan-rh/src/app/rh/candidates/[id]/hire-actions.ts:298 + caftan-rh/src/app/api/docuseal/webhook/route.ts:304,376 + caftan-rh/src/app/rh/dimona/page.tsx:31 + caftan-rh/src/app/planning/employees/[id]/dimona/page.tsx:71
  POURQUOI: La table dimona_declarations est ciblée par DEUX jeux de colonnes mutuellement exclusifs. Cluster A (declaration_kind, start_date, end_date, reference_number, notes ; statuts pending/declared_onss/confirmed) : hire-actions.ts:298-307, contract-actions.ts recordDimonaDeclarationAction:550-562, planning/[id]/dimona/page.tsx:71-73, cron dimona-reminder route.ts:57-59. Cluster B (kind, declared_start_date, declared_end_date, employer_org_key, dimona_period_id, note, declared_by ; statuts pending/declared/failed/cancelled) : docuseal/webhook route.ts:304-311 & 376-384, rh/dimona/page.tsx:31-35, rh/dimona/actions.ts:16-22 & 58-79. Un seul schéma physique peut exister ; l'autre cluster lève « column does not exist » au runtime. Concrètement, selon le schéma live : soit l'insert Dimona IN à l'embauche (hire-actions) + la page /planning/.../dimona + le formulaire RH plantent, soit le dashboard /rh/dimona (SELECT + createDimonaDeclarationAction + markDimonaDeclaredAction) plante en 500. De plus l'upsert webhook utilise onConflict 'employee_id,kind' qui exige un index UNIQUE n'existant que dans le cluster B. La Dimona IN est l'obligation ONSS légale AVANT le 1er jour de travail : cassure de conformité.
  FIX: Choisir UN schéma canonique, écrire une migration ALTER de convergence (renommer declaration_kind<->kind, start_date<->declared_start_date, end_date<->declared_end_date, notes<->note, unifier les valeurs de status, ajouter employer_org_key/dimona_period_id/declared_by/reference_number manquants, créer l'index UNIQUE(employee_id,kind)), puis aligner TOUS les writers/readers (hire-actions, contract-actions.recordDimonaDeclarationAction, dimona-actions, rh/dimona, docuseal/webhook, cron dimona-reminder) sur ces mêmes noms/valeurs. Supprimer la définition en double.

[HIGH] (e2e_coherence) Régime 'partial' vs CHECK 'part' : le bouton « Enregistrer les corrections » du popup d'envoi casse pour tout temps partiel
  FICHIER: caftan-rh/src/app/planning/employees/[id]/contract-terms-actions.ts
  POURQUOI: Sur le chemin critique d'envoi à signer, la carte « Récapitulatif du contrat » écrit employees.work_time_kind via saveContractTermsAction. Or la colonne a une contrainte CHECK ('full','part') (confirmée par la migration 20260703002000 lignes 4 et 28-31, et le commentaire align-profile-actions.ts:47-50). Deux ruptures : (1) ligne 48 la validation n'accepte que 'full'|'partial' — donc si la fiche contient déjà la valeur CANONIQUE 'part', le client la renvoie et l'action REJETTE avec « work_time_kind doit être 'full' ou 'partial' » alors que la donnée est correcte ; (2) si l'opérateur choisit « Temps partiel » le <select> émet 'partial' (sign-contract-button.tsx:342), qui passe la validation puis viole le CHECK DB → « new row violates check constraint ». Résultat : impossible d'enregistrer les paramètres d'un contrat temps partiel (le cas Sanae 24h) depuis le popup ; seul 'full' fonctionne. Le trigger normalize_employee_legal ne rattrape pas car il ne remplit work_time_kind que s'il est NULL.
  FIX: Normaliser vers la convention DB avant l'UPDATE : accepter en entrée 'part'/'partial'/'partiel' et 'full'/'plein', puis mapper 'partial'→'part' (réutiliser le helper normKind déjà écrit dans align-profile-actions.ts:51-56). Corriger la validation ligne 48 pour accepter 'part', et la dérivation ligne 64 pour écrire 'part' au lieu de 'partial'. Idéalement aligner le <select> du popup sur value='part'.

[HIGH] (e2e_coherence) « Aligner la fiche et envoyer » échoue silencieusement sur le régime → divergence fiche/contrat non corrigée + log d'audit mensonger
  FICHIER: caftan-rh/src/app/planning/employees/[id]/sign-contract-actions.ts
  POURQUOI: Dans sendContractForSignatureAction, quand l'opérateur valide les discordances (acceptDiscrepancies), le patch d'alignement écrit à la ligne 471 `patch.work_time_kind = effTpl === 'employee_pt' ? 'partial' : 'full'` — la valeur 'partial' viole le CHECK ('full','part'). L'UPDATE ligne 474 (`admin.from('employees').update(patch)`) n'est PAS vérifié pour erreur. Comme l'update est atomique, dès qu'il y a une discordance de régime, TOUT le patch est rejeté par la DB : ni le régime, ni weekly_hours, ni hourly_rate, ni les dates ne sont alignés. Pourtant activity_log enregistre « Fiche alignée sur le contrat » (ligne 476-482) : le journal d'audit affirme un alignement qui n'a pas eu lieu, et la fiche reste durablement divergente du contrat signé. Le contrat part quand même (rendered_body vient de eff, correct), donc la rupture est totalement silencieuse. Contredit directement la règle « signaler les discordances / aligner ».
  FIX: Écrire 'part' au lieu de 'partial' ligne 471, et vérifier l'erreur retournée par l'UPDATE (si error, ne pas logguer un alignement réussi et remonter l'échec). Mieux : router ce chemin vers alignProfileToContractAction (align-profile-actions.ts) qui gère déjà correctement la normalisation via normKind.

[MEDIUM] (contract_gen) Alignement fiche↔contrat écrit 'partial' (CHECK violé) et l'erreur est ignorée → fiche non alignée mais audit log affirme le contraire
  FICHIER: caftan-rh/src/app/planning/employees/[id]/sign-contract-actions.ts (ligne 471)
  POURQUOI: Dans le chemin acceptDiscrepancies, `patch.work_time_kind = effTpl === 'employee_pt' ? 'partial' : 'full'` écrit 'partial'. L'UPDATE (ligne 474 `await admin.from('employees').update(patch)...`) ne vérifie pas l'erreur retournée. Si une discordance de régime est présente (typique: fiche 'full' héritée de l'ancien trigger 30h vs contrat dérivé part-time à 38h), le patch contient work_time_kind='partial' → violation CHECK → l'UPDATE échoue silencieusement (patch entier rejeté, y compris weekly_hours/dates/etc.). Mais le code insère quand même dans activity_log 'contract_profile_aligned' (ligne 476) et poursuit l'envoi. Conséquence: fiche jamais alignée, faux audit, et la même discordance ré-apparaît à chaque envoi.
  FIX: Écrire 'part' au lieu de 'partial' ligne 471, et vérifier l'erreur de l'UPDATE avant de logguer l'alignement (ne pas prétendre alignement réussi si update.error).

[MEDIUM] (contract_gen) Identifiants légaux employeur (BCE/ONSS/adresse) peuvent apparaître vides sur le contrat signé pour Caftan Factory / Homix
  FICHIER: caftan-rh/src/lib/employer-orgs.ts (toContractEmployerOrg lignes 39-53) + caftan-rh/src/lib/contract-renderer.ts (renderContractTemplate lignes 99-103) + fallback EMPLOYER_ORGS lignes 53-77
  POURQUOI: toContractEmployerOrg mappe les champs DB null vers "" (bce/onss/rc/address). renderContractTemplate remplace toute variable vide par le placeholder '_______________'. Les entités caftan_factory et homix ont des valeurs 'à compléter' dans la constante EMPLOYER_ORGS et leurs lignes employer_orgs peuvent avoir bce/onss null. Aucun garde-fou de complétude d'entité: getMissingFields (contract-readiness) ne vérifie que les champs employé, pas l'entité employeur. Un contrat légalement contraignant peut donc partir à la signature avec employer_bce/employer_onss vides ou en placeholder, ce qui est non conforme (mentions employeur obligatoires en droit belge).
  FIX: Ajouter un contrôle de complétude de l'entité (bce, onss, adresse, représentant, commission paritaire) dans le readiness avant l'envoi à signer, et bloquer l'envoi tant que l'entité choisie n'est pas complète (message 1-clic vers /admin/entities).

[MEDIUM] (esign) La relance de signature (cron) ignore TOTALEMENT le flux interne /sign — dead-end silencieux à l'expiration du token
  FICHIER: src/app/api/cron/signature-reminder/route.ts (l.103-118) + src/app/planning/employees/[id]/sign-contract-actions.ts (l.532-557)
  POURQUOI: Le chemin PROD (sendContractForSignatureAction, /sign interne) insère l'employee_contracts avec signing_token + prepared_at, mais SANS sent_at et SANS docuseal_submission_id. Or le cron de relance filtre les contrats avec `.not('sent_at','is',null).not('docuseal_submission_id','is',null)`. Résultat : aucun contrat envoyé via /sign n'est jamais relancé, ni auto-annulé à J+30. Pire : le token expire à J+14 (l.524-525). Un candidat qui ne signe pas ne reçoit AUCUN rappel, le lien expire en silence, la page affiche « Lien expiré, contacte le RH » mais le RH n'est jamais notifié. Le flux se termine en cul-de-sac invisible — violation directe de la règle « notifier proactivement, ne jamais attendre que l'humain vienne ». De plus, même si un rappel partait, il reconstruit l'URL depuis DocuSeal embed_src (inexistant ici) et retombe sur /me/documents au lieu du lien /sign/[token].
  FIX: Étendre la requête CONTRATS du cron pour inclure les contrats internes : filtrer sur status='ready_to_sign' + signing_token non nul (au lieu d'exiger docuseal_submission_id et sent_at). Construire le lien de relance depuis signing_token (`${base}/sign/${signing_token}`) et non depuis DocuSeal. Ajouter une notif RH 1-clic (renvoyer un lien) quand signing_token_expires_at est dépassé. Alternativement, renseigner sent_at à l'insertion pour au moins réactiver le comptage des jours.

[MEDIUM] (esign) Contrat envoyé avec une signature EMPLOYEUR VIDE si l'admin/RH n'a pas de signature stockée — le mail promet pourtant « les deux signatures »
  FICHIER: src/app/planning/employees/[id]/sign-contract-actions.ts (l.440-516) + src/app/sign/[token]/actions.ts (l.144)
  POURQUOI: sendContractForSignatureAction lit profile.signature_data_url (l.442-446) mais ne vérifie JAMAIS qu'elle est non nulle avant d'envoyer. Si elle est nulle, buildContractHtmlForDocuseal génère un <signature-field role=Employer>, puis l.513-516 remplace TOUT champ signature restant par une simple ligne pointillée vide. Le contrat part donc au candidat avec le côté employeur NON signé (ligne blanche), le candidat signe, et le document final n'a qu'UNE signature. DocuSeal étant dormant, il n'existe aucun mécanisme pour que l'employeur signe ensuite. Le mail de confirmation (sign/actions.ts l.144) et l'écran de succès affirment pourtant « PDF, avec les deux signatures » — affirmation fausse et document employeur légalement non signé.
  FIX: Bloquer l'envoi si employerSignatureDataUrl est null : `if (!employerSignatureDataUrl) return { error: '⛔ Ta signature employeur n'est pas enregistrée. Va dans /admin/settings/my-signature avant d'envoyer le contrat.' }`. Ou, à défaut, ne pas prétendre « deux signatures » dans le mail quand l'employeur n'est pas pré-signé.

[MEDIUM] (esign) Le téléchargement du PDF signé renvoie une erreur 500 brute si PDFSHIFT_API_KEY est absente/indispo (pas de repli), et l'e-mail joint du HTML au lieu du PDF promis
  FICHIER: src/lib/html-to-pdf.ts (l.17-21) + src/app/api/contracts/sign/[token]/pdf/route.ts (l.34-52)
  POURQUOI: renderHtmlToPdf throw si PDFSHIFT_API_KEY manque. La route /api/contracts/sign/[token]/pdf n'a aucun repli : elle renvoie un 500 en texte brut. Or l'écran de succès après signature (sign-client.tsx l.144-149) propose un bouton « Télécharger mon contrat signé (PDF) » qui pointe dessus → le candidat tombe sur une erreur 500 juste après avoir signé. Côté e-mail (sign/actions.ts l.125-136), le repli existe mais joint alors un .html, alors que le corps du mail promet « PDF, avec les deux signatures ». Dépendance à un service tiers = point unique de défaillance sur la livraison du document signé.
  FIX: Dans la route PDF, prévoir un repli : si renderHtmlToPdf échoue, servir le rendered_body en HTML (Content-Type text/html) plutôt qu'un 500. Idéalement stocker le PDF signé au moment de la signature (storage) pour ne pas dépendre de PDFShift à chaque téléchargement, et adapter le texte du mail quand le repli HTML est utilisé.

[MEDIUM] (post_sign) onboarding_run jamais créé lors de la signature électronique interne (/sign)
  FICHIER: caftan-rh/src/app/sign/[token]/actions.ts:79 (submitSignatureAction) + caftan-rh/src/app/planning/employees/[id]/contract-actions.ts:424
  POURQUOI: Le flux prod (sendContractForSignatureAction crée le contrat ready_to_sign avec signing_token, puis submitSignatureAction le passe à 'signed', active le compte et envoie les mails) n'instancie JAMAIS d'onboarding_run/onboarding_run_items. La seule création est dans markContractSignedAction (contract-actions.ts:424-477), protégée par `if (c.status !== "ready_to_sign") return { error }` et déclenchée uniquement par le bouton admin « marquer signé ». Une fois l'employé signataire via le lien magique, le statut est déjà 'signed' → markContractSignedAction est injoignable (renvoie une erreur). Résultat : tout employé qui signe réellement via /sign obtient un /me/onboarding VIDE ; le détecteur d'anomalies signalera plus tard « onboarding non terminé » sans run à compléter. Aucun trigger DB de secours (aucune migration onboarding présente).
  FIX: Déplacer/dupliquer le bloc d'instanciation onboarding_run (contract-actions.ts:424-477) dans submitSignatureAction (après activateEmployeeAccount), idempotent (créer seulement si l'employé n'a pas déjà de run). Alternativement l'instancier au plus tard dans sendContractForSignatureAction au moment où le contrat passe ready_to_sign.

[MEDIUM] (post_sign) work_time_kind='partial' viole la contrainte CHECK et perd silencieusement l'alignement fiche<->contrat
  FICHIER: caftan-rh/src/app/planning/employees/[id]/sign-contract-actions.ts:471
  POURQUOI: Lors de l'envoi à signer, si l'opérateur valide une discordance work_time_kind (acceptDiscrepancies), le code fait `patch.work_time_kind = effTpl === 'employee_pt' ? 'partial' : 'full'`. La contrainte CHECK exige 'part' (voir migration 20260703002000_fix_normalize_employee_legal_hours.sql:4 « écrivait 'partial' alors que la contrainte CHECK exige 'part' (violation) »). L'`admin.from('employees').update(patch)` (ligne 474) ne vérifie pas l'erreur retournée : l'UPDATE est rejeté par le CHECK et l'alignement est perdu en silence — la fiche et le contrat restent divergents alors que l'opérateur a validé l'alignement. Contredit la règle « signaler la discordance, aligner après validation ».
  FIX: Remplacer 'partial' par 'part' à la ligne 471 (conforme au CHECK). Vérifier l'erreur de l'UPDATE et remonter/journaliser tout échec au lieu de l'ignorer.

[MEDIUM] (e2e_coherence) Aperçu ≠ document signé sur l'entité employeur si l'opérateur change « Entité émettrice »
  FICHIER: caftan-rh/src/app/planning/employees/[id]/contract-preview/action.ts
  POURQUOI: Le WYSIWYG (preview = doc signé) est garanti uniquement dans le cas par défaut. La preview (previewContractHtmlAction) résout TOUJOURS l'entité depuis le SITE du travailleur : resolveEmployerOrgForEmployee(employeeId) (contract-preview/action.ts:40), et le lien d'aperçu ne transmet que ?tpl= sans orgKey (sign-contract-button.tsx:283). L'envoi réel, lui, utilise l'orgKey choisi dans le dialogue (sign-contract-actions.ts:492 getEmployerOrg(args.orgKey) → employerData). Comme le builder fait `org = args.employerData ?? EMPLOYER_ORGS[...]` (docuseal-flow.ts:944), les VALEURS employeur (raison sociale, adresse, BCE) du document signé suivent l'entité sélectionnée, tandis que l'aperçu montre toujours l'entité du site. Si l'opérateur clique une autre entité que le défaut (AMD/Caftan Factory/Homix), il signe un contrat avec une entité que l'aperçu n'a jamais montrée — sur un document légal, l'employeur nommé est critique.
  FIX: Propager l'orgKey sélectionné au lien d'aperçu (ex. ?tpl=..&org=..) et faire accepter cet override à previewContractHtmlAction, OU verrouiller l'entité sur celle du site (retirer les boutons de switch) pour garantir preview=signé. Dans tous les cas, une seule source pour l'entité entre les deux chemins.

[LOW] (hire_path) Filet de secours d'embauche degrade : l'insert direct omet IBAN, NISS, banque, naissance et adresse
  FICHIER: C:/Users/KElba/Documents/GitHub/Formulaire_candidats/caftan-rh/src/app/rh/candidates/[id]/hire-actions.ts
  POURQUOI: Quand le trigger n'a pas cree la row employees (boucle d'attente epuisee, lignes 135-144), le fallback d'insertion directe (146-173) ne renseigne que candidate_id, application_id, email, full_name, phone, job_title, contract_type, weekly_hours, start_date, end_date, status. Il n'insere PAS iban, bic, bank_holder, nrn, birth_date, birth_place, address, postal_code, city (ni nationalite/etat civil/etudiant/transport). Contrairement au trigger, ce chemin cree donc un employe amute de toute son identite bancaire et administrative, silencieusement (le step est marque 'ok'). Le contrat prepare juste apres (prepareContractAction) et la Dimona partent alors avec un dossier incomplet, sans aucune alerte.
  FIX: Aligner l'insert direct de secours sur les memes colonnes que le trigger (recharger cand.* et copier iban, bic, bank_holder, nrn, birth_date, birth_place, address, postal_code, city, + is_student/transport). A defaut, signaler un step 'warn' explicite que la fiche est incomplete.

[LOW] (hire_path) UPDATE employees post-trigger sans controle d'erreur : conversion CDI->CDD et rejets CHECK passent inapercus
  FICHIER: C:/Users/KElba/Documents/GitHub/Formulaire_candidats/caftan-rh/src/app/rh/candidates/[id]/hire-actions.ts
  POURQUOI: Lignes 176-185 : l'UPDATE qui applique le contract_type, weekly_hours, dates et job_title choisis dans le dialog n'a AUCUNE verification d'erreur (le resultat n'est pas lu). Le dialog propose 'CDI', 'Intérim' et 'Freelance' (hire-button.tsx:41). Pour un CDI, hire-actions force end_date=null (ligne 76), puis le trigger de normalisation legale reecrit silencieusement contract_type CDI->CDD en gardant end_date null (CDD sans terme = juridiquement incoherent) — sans retour a l'ecran. Et si une contrainte CHECK rejette 'Intérim'/'Freelance', l'echec est avale : l'employe reste avec le contract_type par defaut du trigger et la RH croit son choix applique. Ces valeurs alimentent ensuite le contrat prepare et la Dimona.
  FIX: Lire l'erreur de l'UPDATE et la remonter dans steps (warn/error). Retirer 'CDI' du dialog si la politique no-CDI est active (ou avertir de la conversion + forcer un end_date par defaut), et verifier que 'Intérim'/'Freelance' sont acceptes par la contrainte CHECK avant de les proposer.

[LOW] (hire_path) worker_type Dimona derive du type de contrat du dialog, pas du statut etudiant auto-declare
  FICHIER: C:/Users/KElba/Documents/GitHub/Formulaire_candidats/caftan-rh/src/app/rh/candidates/[id]/hire-actions.ts
  POURQUOI: Ligne 304 : worker_type = contractKind === 'Étudiant' ? 'STU' : 'OTH'. Le defaut du dialog est 'CDD' (hire-button.tsx:62). Si la RH laisse le defaut pour un candidat qui s'est declare etudiant (candidates.is_student=true, jamais consulte ici), la Dimona part en 'OTH' -> declaration ONSS erronee, cotisations reduites etudiant perdues, contingent 600h non trace. 'Intérim' et 'Freelance' tombent aussi en 'OTH' alors qu'ils exigent un autre traitement (voire aucune Dimona pour un freelance).
  FIX: Deriver worker_type en croisant candidate.is_student ET le type de contrat ; pre-selectionner 'Étudiant' dans le dialog quand is_student=true et alerter en cas de divergence ; mapper Intérim/Freelance vers le worker_type correct ou bloquer la Dimona pour un freelance.

[LOW] (contract_gen) Entité employeur de la preview ≠ entité du document signé (et représentant différent) → rupture WYSIWYG sur l'entité
  FICHIER: caftan-rh/src/app/planning/employees/[id]/contract-preview/action.ts (lignes 39-52) vs sign-contract-actions.ts (lignes 447-503)
  POURQUOI: La preview résout l'entité UNIQUEMENT via resolveEmployerOrgForEmployee (entité du site) et passe employerRepresentativeOverride = profile.full_name brut. L'envoi à signer utilise l'entité sélectionnable par l'opérateur (args.orgKey, défaut = site) via getEmployerOrg(args.orgKey), et employerRepresentativeOverride = pickAuthorizedRepresentative(profile.full_name, orgInfo). Deux divergences: (1) si l'opérateur bascule le toggle 'Entité émettrice' du dialog vers une autre entité, le document signé porte cette entité alors que le lien Aperçu (contract-preview?tpl=...) montre toujours l'entité du site — le WYSIWYG revendiqué est faux pour l'entité; (2) le nom du représentant affiché dans la preview (profile.full_name) peut différer de celui du doc signé (pickAuthorizedRepresentative retombe sur le représentant officiel si le connecté n'est pas autorisé).
  FIX: Faire passer le orgKey sélectionné (et le même override de représentant via pickAuthorizedRepresentative) au lien/aperçu, ou verrouiller l'entité sur celle du site des deux côtés, afin que preview et document signé partagent strictement les mêmes inputs entité/représentant.

[LOW] (contract_gen) Option 'CDI' encore proposée dans le popup et acceptée par saveContractTermsAction, malgré la politique 'jamais de CDI'
  FICHIER: caftan-rh/src/app/planning/employees/[id]/sign-contract-button.tsx (ligne 328) + contract-terms-actions.ts (lignes 41-44)
  POURQUOI: Le select 'Type de contrat' propose <option value="CDI">, et saveContractTermsAction accepte n'importe quelle chaîne non vide comme contract_type. Cela contredit la politique métier 'jamais de CDI' (contract-renderer.ts lignes 148-154). La conformité repose uniquement sur le trigger DB (no_cdi_policy) pour reconvertir CDI→CDD, ce qui est fragile si la règle est désactivée. Accessoirement, le mail d'archive employeur utilise orgInfo.name issu de la constante EMPLOYER_ORGS (ex: 'CAFTAN FACTORY') tandis que le document utilise le name de la ligne employer_orgs — possible incohérence de dénomination entre mail et doc.
  FIX: Retirer l'option CDI du select (ou la rejeter côté saveContractTermsAction), et utiliser le name de l'entité DB (orgRow.name) aussi pour le mail d'archive.

[LOW] (esign) Pas de garde atomique contre la double signature (read-then-write) — double activation / double e-mail possible en cas de soumissions concurrentes
  FICHIER: src/app/sign/[token]/actions.ts (l.51-79)
  POURQUOI: submitSignatureAction relit le contrat, vérifie status/signed_at (l.51-53), PUIS fait l'update (l.65-74) sans condition atomique (pas de `.eq('status','ready_to_sign')` ni de garde WHERE signed_at IS NULL). Deux soumissions vraiment concurrentes (même lien ouvert sur deux appareils, réseau lent) peuvent toutes deux passer le pré-check et exécuter l'update, déclenchant deux fois activateEmployeeAccount (idempotent, OK) et surtout deux envois du contrat signé au candidat. Le bouton client est désactivé pendant `pending`, ce qui limite le risque au cas concurrent réel.
  FIX: Rendre l'update conditionnel et idempotent : `.update({...}).eq('id', contractId).is('signed_at', null)` et ne poursuivre (activation + mails) que si une ligne a réellement été mise à jour (rowCount 1).

[LOW] (esign) Le schéma de employee_contracts et ses colonnes de signature n'ont AUCUNE migration commitée (créées hors-bande)
  FICHIER: supabase/migrations/ (aucune définition de employee_contracts.signing_token / employee_signature_png / signed_ip / rendered_body / status)
  POURQUOI: Grep sur les migrations SQL : aucune ne crée employee_contracts ni ses colonnes clés (signing_token, signing_token_expires_at, employee_signature_png, signed_ip, rendered_body, status 'ready_to_sign'/'signed'). La table n'est référencée qu'incidemment dans une migration de normalisation. La prod fonctionne parce que le schéma a été appliqué directement dans Supabase, mais il n'est pas reproductible ni rollback-able depuis le repo — correspond au pattern documenté « fichiers/objets jamais commités ». Risque de dérive schéma entre environnements et de perte de la garantie de rollback exigée par les règles projet.
  FIX: Ajouter une migration idempotente (create table if not exists / add column if not exists + contrainte CHECK sur status) reflétant le schéma réel de employee_contracts, pour aligner le repo sur la prod et préserver la capacité de rollback.

[LOW] (post_sign) Alerte « DIMONA URGENTE » et rappel mail RH ne se déclenchent que dans le webhook DocuSeal dormant, ignoré pour le flux interne
  FICHIER: caftan-rh/src/app/api/docuseal/webhook/route.ts:100-114 (garde isInternalFlow) + :391-447 + caftan-rh/src/app/sign/[token]/actions.ts:90-106
  POURQUOI: Toute la logique d'alerte aiguë post-signature (notifications in-app dimona_to_do pour admin/rh, push, mail de rappel « Dimona IN avant le 1er jour » + auto-création Dimona IN) vit dans le webhook DocuSeal (route.ts:367-447). Or ce webhook retourne tôt (ignored) pour tout contrat du flux interne via la garde isInternalFlow (signing_token présent ou pas de docuseal_submission_id, route.ts:100-114). Dans le flux prod /sign, submitSignatureAction n'envoie qu'un mail générique « X a signé » à Karim (actions.ts:90-106), sans notification/push/mail Dimona-IN au moment exact du déclencheur légal. Le RH ne dépend plus que du cron quotidien + du dashboard — dashboard potentiellement cassé (cf. finding #1).
  FIX: Ajouter dans submitSignatureAction, après activation, la création des notifications dimona_to_do + push + mail de rappel RH (réutiliser le bloc route.ts:391-447), et l'upsert Dimona IN une fois le schéma unifié. Ne pas dépendre du webhook DocuSeal qui est dormant pour les contrats internes.

[LOW] (e2e_coherence) Représentant signataire de l'aperçu (nom du profil brut) peut différer du représentant du contrat signé (représentant autorisé)
  FICHIER: caftan-rh/src/app/planning/employees/[id]/contract-preview/action.ts
  POURQUOI: La preview passe employerRepresentativeOverride = profile.full_name ?? 'Karim Elbazi' (contract-preview/action.ts:52), sans contrôle d'autorisation. L'envoi passe pickAuthorizedRepresentative(profile.full_name, orgInfo) (sign-contract-actions.ts:448) qui retombe sur le représentant légal officiel de l'entité si le nom connecté n'est pas dans [representative, co_representative]. Donc un utilisateur RH dont le nom n'est pas déclaré verra son propre nom comme signataire dans l'aperçu, alors que le contrat signé portera le représentant officiel (Karim) — nom du signataire employeur divergent entre aperçu et document légal signé. Impact réel faible car l'app est opérée par Karim, mais c'est une entorse au WYSIWYG sur une mention légale.
  FIX: Utiliser pickAuthorizedRepresentative(profile.full_name, EMPLOYER_ORGS[orgKey]) aussi dans previewContractHtmlAction, pour que l'aperçu montre exactement le représentant qui figurera sur le document signé.
